import 'package:flutter/cupertino.dart';

class Category{
  String title,description,imageUrl;
  Color backgroundColor,fontColor;

  Category(this.title, this.description, this.imageUrl, this.backgroundColor,
      this.fontColor);
}